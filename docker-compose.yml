version: '3.9'
services:
  db:
    image: postgres:15
    container_name: db_app
    env_file:
      - .env

  app:
    build:
      context: .
    env_file:
      - .env
    container_name: fastapi_glpi
    command: ["/fastapi_glpi/docker/app.sh"]
    ports:
      - 9999:8000
    depends_on:
      - db